[project]
name = "GeminiQuotaApp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.33.1",
    "rumps>=0.4.0",
]

[dependency-groups]
dev = [
    "py2app>=0.28.7",
    "setuptools>=70",
]
